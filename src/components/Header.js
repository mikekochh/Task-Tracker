import React from 'react';

const Header = () => {
    return (
        <div style={{textAlign: "center"}}>
            <h2>Task Tracker</h2>
            <br></br>
        </div>
    );
};

export default Header;